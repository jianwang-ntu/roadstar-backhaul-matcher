# Version RANGES, not pins. The exact versions every number in README.md was
# produced under, recorded here because the file used to call itself "pinned"
# while specifying ranges (round-2 audit finding CLAIMS-08):
#   python 3.13.9   numpy 2.2.6   scipy 1.16.3   pytest 9.0.3
numpy>=1.26,<3
scipy>=1.11,<2
pytest>=7.4
